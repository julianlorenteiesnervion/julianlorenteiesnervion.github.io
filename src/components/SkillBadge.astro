---
interface Props {
    name: string;
}

const { name } = Astro.props;
---

<span class="px-3 py-1 bg-gray-800 hover:bg-gray-700 text-gray-200 text-sm rounded-md border border-gray-700 transition-all duration-200 hover:scale-105 cursor-default">
    {name}
</span>
